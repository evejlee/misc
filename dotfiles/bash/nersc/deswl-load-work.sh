#!/bin/bash
#
# load work versions of esutil, desdb, and deswl

module unload esutil && module load esutil/work
module unload desdb && module load desdb/work
module unload deswl && module load deswl/work
