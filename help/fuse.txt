tried 
  vicgr to add esheldon to fuse group
    (also had to do shadow file)
  sudo chgrp fuse /dev/fuse
  sudo chmod 666 /dev/fuse

now have error: 
	fuse: failed to exec fusermount: Permission denied

had to do this:
     sudo chmod +x /bin/fusermount

Says I might have to repeat after reboot?
